// MonsterSystem stub - manages monster data and persistence.
// Responsibilities: monster stats, leveling, modifiers, and lookup.

export default class MonsterSystem {
  constructor() {}

  // Future: createMonster(), applyDamage(), getStats()
}
